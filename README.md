## Simple project to test network
* With that project you can do network troubleshooting easily. The small project can be upload on AWS Lambda Console.

### How To:
* Execute `build.sh` to more instructions;
